//! AC2: --help is honest. Stage-2 scaffold stub; iter-2 lands the real grep.

#[test]
#[ignore = "iter-1 scaffold: --help body settles in iter-2"]
fn help_lists_all_flags() {
    // Will assert that `agentns-claude --help` lists --intent, --budget,
    // --no-unshare, --verbose, the mock-mode contract, and the --no-unshare
    // fallback. Implementation in iter-2.
}
